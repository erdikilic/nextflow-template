extends: default

rules:
  document-start: disable
  line-length:
    max: 120
    allow-non-breakable-words: true
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  indentation:
    spaces: 2
    indent-sequences: true

ignore: |
  .nf-test/
  work/
