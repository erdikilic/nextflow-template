{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/erdikilic/nextflow-template/main/nextflow_schema.json",
  "title": "nextflow-template pipeline parameters",
  "description": "A state-of-the-art Nextflow DSL2 pipeline template.",
  "type": "object",
  "$defs": {
    "input_output_options": {
      "title": "Input/output options",
      "type": "object",
      "fa_icon": "fas fa-terminal",
      "description": "Define where the pipeline should find input data and save output data.",
      "required": ["input", "outdir"],
      "properties": {
        "input": {
          "type": "string",
          "format": "file-path",
          "exists": true,
          "mimetype": "text/csv",
          "pattern": "^\\S+\\.csv$",
          "schema": "assets/schema_input.json",
          "description": "Path to a comma-separated samplesheet describing the input samples.",
          "help_text": "Use `assets/samplesheet.csv` as a starting point. Columns: sample, platform, fastq_1, fastq_2.",
          "fa_icon": "fas fa-file-csv"
        },
        "outdir": {
          "type": "string",
          "format": "directory-path",
          "description": "The output directory where results are saved.",
          "fa_icon": "fas fa-folder-open"
        }
      }
    },
    "generic_options": {
      "title": "Generic options",
      "type": "object",
      "fa_icon": "fas fa-cog",
      "description": "Less common options for the pipeline, typically set in a config file.",
      "properties": {
        "help": {
          "type": "boolean",
          "description": "Display help text.",
          "fa_icon": "fas fa-question-circle"
        },
        "publish_dir_mode": {
          "type": "string",
          "default": "copy",
          "description": "Method used to save pipeline results to the output directory.",
          "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
          "fa_icon": "fas fa-copy"
        },
        "validate_params": {
          "type": "boolean",
          "default": true,
          "description": "Validate parameters against the schema at runtime.",
          "fa_icon": "fas fa-check-square"
        }
      }
    },
    "max_job_request_options": {
      "title": "Max job request options",
      "type": "object",
      "fa_icon": "fab fa-acquisitions-incorporated",
      "description": "Set the top limit for requested resources for any single job.",
      "properties": {
        "max_cpus": {
          "type": "integer",
          "default": 14,
          "description": "Maximum number of CPUs that can be requested for any single job.",
          "fa_icon": "fas fa-microchip"
        },
        "max_memory": {
          "type": "string",
          "default": "30.GB",
          "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
          "description": "Maximum amount of memory that can be requested for any single job.",
          "fa_icon": "fas fa-memory"
        }
      }
    }
  },
  "allOf": [
    { "$ref": "#/$defs/input_output_options" },
    { "$ref": "#/$defs/generic_options" },
    { "$ref": "#/$defs/max_job_request_options" }
  ]
}
